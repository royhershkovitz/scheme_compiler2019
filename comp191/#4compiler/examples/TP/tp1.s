
;;; All tconst_tbl and the macros and the scheme-object printing procedure
;;; are defined in compiler.s
%include "compiler.s"
%include "prims.s"
section .bss
malloc_pointer:
    resq 1

section .data
const_tbl:
  dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
fvar_tbl:
  dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
symbols_tbl:
  dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
	dq T_UNDEFINED
global main
not_enough_ram:
    mov rax, 0
	  mov rdi, .exit_malloc
	  call printf

    mov rax, 60       
    mov rdi, -1     ;EXIT_Failure
    syscall
section .data
.exit_malloc: 
    db "Failure! not enough ram!",0 

section .text
main:
    ;; set up the heap
    mov rdi, GB(1)
    call malloc
    cmp rax, 0
    je not_enough_ram
    mov [malloc_pointer], rax

    ;; Set up the dummy activation frame
    ;; The dummy return address is T_UNDEFINED
    ;; (which a is a macro for 0) so that returning
    ;; from the top level (which SHOULD NOT HAPPEN
    ;; AND IS A BUG) will cause a segfault.
    push qword 0
    push qword SOB_EMPTY_ENV

    call code_fragment
    add rsp, 2*8
    ret

code_fragment:
    ;; Set up the primitive stdlib fvars:
    ;; Since the primtive procedures are defined in assembly,
    ;; they are not generated by scheme (define ...) expressions.
    ;; This is where we emulate the missing (define ...) expressions
    ;; for all the primitive procedures.
    push rbp
    mov rbp, rsp

jmp sympass0
	sym0:
		db 98,111,120, 0
	sympass0:
	mov qword[symbols_tbl+0*WORD_SIZE], sym0
  jmp sympass1
	sym1:
		db 117,110,98,111,120, 0
	sympass1:
	mov qword[symbols_tbl+1*WORD_SIZE], sym1
  jmp sympass2
	sym2:
		db 115,101,116,45,98,111,120,33, 0
	sympass2:
	mov qword[symbols_tbl+2*WORD_SIZE], sym2
  jmp sympass3
	sym3:
		db 97,112,112,108,121, 0
	sympass3:
	mov qword[symbols_tbl+3*WORD_SIZE], sym3
  jmp sympass4
	sym4:
		db 99,97,114, 0
	sympass4:
	mov qword[symbols_tbl+4*WORD_SIZE], sym4
  jmp sympass5
	sym5:
		db 99,100,114, 0
	sympass5:
	mov qword[symbols_tbl+5*WORD_SIZE], sym5
  jmp sympass6
	sym6:
		db 99,111,110,115, 0
	sympass6:
	mov qword[symbols_tbl+6*WORD_SIZE], sym6
  jmp sympass7
	sym7:
		db 115,101,116,45,99,97,114,33, 0
	sympass7:
	mov qword[symbols_tbl+7*WORD_SIZE], sym7
  jmp sympass8
	sym8:
		db 115,101,116,45,99,100,114,33, 0
	sympass8:
	mov qword[symbols_tbl+8*WORD_SIZE], sym8
  jmp sympass9
	sym9:
		db 98,111,111,108,101,97,110,63, 0
	sympass9:
	mov qword[symbols_tbl+9*WORD_SIZE], sym9
  jmp sympass10
	sym10:
		db 102,108,111,97,116,63, 0
	sympass10:
	mov qword[symbols_tbl+10*WORD_SIZE], sym10
  jmp sympass11
	sym11:
		db 105,110,116,101,103,101,114,63, 0
	sympass11:
	mov qword[symbols_tbl+11*WORD_SIZE], sym11
  jmp sympass12
	sym12:
		db 112,97,105,114,63, 0
	sympass12:
	mov qword[symbols_tbl+12*WORD_SIZE], sym12
  jmp sympass13
	sym13:
		db 110,117,108,108,63, 0
	sympass13:
	mov qword[symbols_tbl+13*WORD_SIZE], sym13
  jmp sympass14
	sym14:
		db 99,104,97,114,63, 0
	sympass14:
	mov qword[symbols_tbl+14*WORD_SIZE], sym14
  jmp sympass15
	sym15:
		db 118,101,99,116,111,114,63, 0
	sympass15:
	mov qword[symbols_tbl+15*WORD_SIZE], sym15
  jmp sympass16
	sym16:
		db 115,116,114,105,110,103,63, 0
	sympass16:
	mov qword[symbols_tbl+16*WORD_SIZE], sym16
  jmp sympass17
	sym17:
		db 112,114,111,99,101,100,117,114,101,63, 0
	sympass17:
	mov qword[symbols_tbl+17*WORD_SIZE], sym17
  jmp sympass18
	sym18:
		db 115,121,109,98,111,108,63, 0
	sympass18:
	mov qword[symbols_tbl+18*WORD_SIZE], sym18
  jmp sympass19
	sym19:
		db 115,116,114,105,110,103,45,108,101,110,103,116,104, 0
	sympass19:
	mov qword[symbols_tbl+19*WORD_SIZE], sym19
  jmp sympass20
	sym20:
		db 115,116,114,105,110,103,45,114,101,102, 0
	sympass20:
	mov qword[symbols_tbl+20*WORD_SIZE], sym20
  jmp sympass21
	sym21:
		db 115,116,114,105,110,103,45,115,101,116,33, 0
	sympass21:
	mov qword[symbols_tbl+21*WORD_SIZE], sym21
  jmp sympass22
	sym22:
		db 109,97,107,101,45,115,116,114,105,110,103, 0
	sympass22:
	mov qword[symbols_tbl+22*WORD_SIZE], sym22
  jmp sympass23
	sym23:
		db 118,101,99,116,111,114,45,108,101,110,103,116,104, 0
	sympass23:
	mov qword[symbols_tbl+23*WORD_SIZE], sym23
  jmp sympass24
	sym24:
		db 118,101,99,116,111,114,45,114,101,102, 0
	sympass24:
	mov qword[symbols_tbl+24*WORD_SIZE], sym24
  jmp sympass25
	sym25:
		db 118,101,99,116,111,114,45,115,101,116,33, 0
	sympass25:
	mov qword[symbols_tbl+25*WORD_SIZE], sym25
  jmp sympass26
	sym26:
		db 109,97,107,101,45,118,101,99,116,111,114, 0
	sympass26:
	mov qword[symbols_tbl+26*WORD_SIZE], sym26
  jmp sympass27
	sym27:
		db 115,121,109,98,111,108,45,62,115,116,114,105,110,103, 0
	sympass27:
	mov qword[symbols_tbl+27*WORD_SIZE], sym27
  jmp sympass28
	sym28:
		db 99,104,97,114,45,62,105,110,116,101,103,101,114, 0
	sympass28:
	mov qword[symbols_tbl+28*WORD_SIZE], sym28
  jmp sympass29
	sym29:
		db 105,110,116,101,103,101,114,45,62,99,104,97,114, 0
	sympass29:
	mov qword[symbols_tbl+29*WORD_SIZE], sym29
  jmp sympass30
	sym30:
		db 101,113,63, 0
	sympass30:
	mov qword[symbols_tbl+30*WORD_SIZE], sym30
  jmp sympass31
	sym31:
		db 43, 0
	sympass31:
	mov qword[symbols_tbl+31*WORD_SIZE], sym31
  jmp sympass32
	sym32:
		db 42, 0
	sympass32:
	mov qword[symbols_tbl+32*WORD_SIZE], sym32
  jmp sympass33
	sym33:
		db 45, 0
	sympass33:
	mov qword[symbols_tbl+33*WORD_SIZE], sym33
  jmp sympass34
	sym34:
		db 47, 0
	sympass34:
	mov qword[symbols_tbl+34*WORD_SIZE], sym34
  jmp sympass35
	sym35:
		db 60, 0
	sympass35:
	mov qword[symbols_tbl+35*WORD_SIZE], sym35
  jmp sympass36
	sym36:
		db 61, 0
	sympass36:
	mov qword[symbols_tbl+36*WORD_SIZE], sym36
  jmp sympass37
	sym37:
		db 101,113,117,97,108,63, 0
	sympass37:
	mov qword[symbols_tbl+37*WORD_SIZE], sym37
  jmp sympass38
	sym38:
		db 110,111,116, 0
	sympass38:
	mov qword[symbols_tbl+38*WORD_SIZE], sym38
  jmp sympass39
	sym39:
		db 62, 0
	sympass39:
	mov qword[symbols_tbl+39*WORD_SIZE], sym39
  jmp sympass40
	sym40:
		db 108,101,110,103,116,104, 0
	sympass40:
	mov qword[symbols_tbl+40*WORD_SIZE], sym40
  jmp sympass41
	sym41:
		db 118,101,99,116,111,114, 0
	sympass41:
	mov qword[symbols_tbl+41*WORD_SIZE], sym41
  jmp sympass42
	sym42:
		db 108,105,115,116,45,62,118,101,99,116,111,114, 0
	sympass42:
	mov qword[symbols_tbl+42*WORD_SIZE], sym42
  jmp sympass43
	sym43:
		db 118,101,99,116,111,114,45,62,108,105,115,116, 0
	sympass43:
	mov qword[symbols_tbl+43*WORD_SIZE], sym43
  jmp sympass44
	sym44:
		db 109,97,112, 0
	sympass44:
	mov qword[symbols_tbl+44*WORD_SIZE], sym44
  jmp sympass45
	sym45:
		db 110,117,109,98,101,114,63, 0
	sympass45:
	mov qword[symbols_tbl+45*WORD_SIZE], sym45
  jmp sympass46
	sym46:
		db 108,105,115,116,63, 0
	sympass46:
	mov qword[symbols_tbl+46*WORD_SIZE], sym46
  jmp sympass47
	sym47:
		db 108,105,115,116, 0
	sympass47:
	mov qword[symbols_tbl+47*WORD_SIZE], sym47
  jmp sympass48
	sym48:
		db 122,101,114,111,63, 0
	sympass48:
	mov qword[symbols_tbl+48*WORD_SIZE], sym48
  jmp sympass49
	sym49:
		db 97,112,112,101,110,100, 0
	sympass49:
	mov qword[symbols_tbl+49*WORD_SIZE], sym49
  jmp sympass50
	sym50:
		db 115,101,116,45,117,110,98,111,120, 0
	sympass50:
	mov qword[symbols_tbl+50*WORD_SIZE], sym50
      mov rax, simples_const_tbl
      mov [const_tbl+0], rax
      mov rax, simples_const_tbl + 1
      mov [const_tbl+8], rax
      mov rax, simples_const_tbl + 2
      mov [const_tbl+16], rax
      mov rax, simples_const_tbl + 4
      mov [const_tbl+24], rax
      jmp string_pass1
string1:
	db T_STRING
	dq 8
	db 119,104,97,116,101,118,101,114
string_pass1:
mov rax, string1
      mov [const_tbl+32], rax
      mov rbx, [const_tbl+32]
		MAKE_SYMBOL (rax, rbx)
      mov [const_tbl+40], rax
      MAKE_INT (rax,0)
      mov [const_tbl+48], rax
      MAKE_INT (rax,1)
      mov [const_tbl+56], rax
      jmp string_pass2
string2:
	db T_STRING
	dq 57
	db 116,104,105,115,32,115,104,111,117,108,100,32,98,101,32,97,110,32,101,114,114,111,114,44,32,98,117,116,32,121,111,117,32,100,111,110,39,116,32,115,117,112,112,111,114,116,32,101,120,99,101,112,116,105,111,110,115
string_pass2:
mov rax, string2
      mov [const_tbl+64], rax
      MAKE_CHAR (rax,0)
      mov [const_tbl+72], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, box)
    mov [fvar_tbl + WORD_SIZE*0], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, get_car)
    mov [fvar_tbl + WORD_SIZE*1], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, set_car)
    mov [fvar_tbl + WORD_SIZE*2], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, apply)
    mov [fvar_tbl + WORD_SIZE*3], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, get_car)
    mov [fvar_tbl + WORD_SIZE*4], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, get_cdr)
    mov [fvar_tbl + WORD_SIZE*5], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, cons)
    mov [fvar_tbl + WORD_SIZE*6], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, set_car)
    mov [fvar_tbl + WORD_SIZE*7], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, set_cdr)
    mov [fvar_tbl + WORD_SIZE*8], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, is_boolean)
    mov [fvar_tbl + WORD_SIZE*9], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, is_float)
    mov [fvar_tbl + WORD_SIZE*10], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, is_integer)
    mov [fvar_tbl + WORD_SIZE*11], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, is_pair)
    mov [fvar_tbl + WORD_SIZE*12], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, is_null)
    mov [fvar_tbl + WORD_SIZE*13], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, is_char)
    mov [fvar_tbl + WORD_SIZE*14], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, is_vector)
    mov [fvar_tbl + WORD_SIZE*15], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, is_string)
    mov [fvar_tbl + WORD_SIZE*16], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, is_procedure)
    mov [fvar_tbl + WORD_SIZE*17], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, is_symbol)
    mov [fvar_tbl + WORD_SIZE*18], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, string_length)
    mov [fvar_tbl + WORD_SIZE*19], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, string_ref)
    mov [fvar_tbl + WORD_SIZE*20], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, string_set)
    mov [fvar_tbl + WORD_SIZE*21], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, make_string)
    mov [fvar_tbl + WORD_SIZE*22], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, vector_length)
    mov [fvar_tbl + WORD_SIZE*23], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, vector_ref)
    mov [fvar_tbl + WORD_SIZE*24], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, vector_set)
    mov [fvar_tbl + WORD_SIZE*25], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, make_vector)
    mov [fvar_tbl + WORD_SIZE*26], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, symbol_to_string)
    mov [fvar_tbl + WORD_SIZE*27], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, char_to_integer)
    mov [fvar_tbl + WORD_SIZE*28], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, integer_to_char)
    mov [fvar_tbl + WORD_SIZE*29], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, is_eq)
    mov [fvar_tbl + WORD_SIZE*30], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, bin_add)
    mov [fvar_tbl + WORD_SIZE*31], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, bin_mul)
    mov [fvar_tbl + WORD_SIZE*32], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, bin_sub)
    mov [fvar_tbl + WORD_SIZE*33], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, bin_div)
    mov [fvar_tbl + WORD_SIZE*34], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, bin_lt)
    mov [fvar_tbl + WORD_SIZE*35], rax
      MAKE_CLOSURE(rax, SOB_NIL_ADDRESS, bin_equ)
    mov [fvar_tbl + WORD_SIZE*36], rax
love:
		
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody1)
		jmp doneclousure1
		lambody1:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 2
    push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:val
    push rax
			mov rax, PVAR(0);get param:box
    push rax
    
    mov rbx, fvar_tbl + WORD_SIZE*7
       mov rax, qword [rbx];get fvar:set-car!
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    leave
    ret
		doneclousure1:
    mov qword [fvar_tbl + WORD_SIZE*2], rax;setting fvar:set-box!
    mov rax, SOB_VOID_ADDRESS
		call write_sob_if_not_void

		
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody2)
		jmp doneclousure2
		lambody2:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:box
    push rax
    
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 1
    REST_APPLICTP
    leave
    ret
		doneclousure2:
    mov qword [fvar_tbl + WORD_SIZE*50], rax;setting fvar:set-unbox
    mov rax, SOB_VOID_ADDRESS
		call write_sob_if_not_void

		
    push SOB_NIL_ADDRESS
			mov rbx, fvar_tbl + WORD_SIZE*6
       mov rax, qword [rbx];get fvar:cons
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*5
       mov rax, qword [rbx];get fvar:cdr
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*13
       mov rax, qword [rbx];get fvar:null?
		IS_UNDEF rbx
    push rax
    push qword 4
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody3)
		jmp doneclousure3
		lambody3:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 4
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody4)
		jmp doneclousure4
		lambody4:
		push rbp
    mov rbp, rsp
    OPT_MAKE_LIST_IF_NEEDED 0
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:args
    push rax
			mov rax, [const_tbl + 8]
    push rax
    
    push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 40]
    push rax
    push qword 1
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody5)
		jmp doneclousure5
		lambody5:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:f
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*0
       mov rax, qword [rbx];get fvar:box
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov PVAR(0), rax;setting param:f
    mov rax, SOB_VOID_ADDRESS
		push SOB_NIL_ADDRESS
			EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody6)
		jmp doneclousure6
		lambody6:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 2
    push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:args
    push rax
    push qword 1
    GET_BOUND rax,2,0;get bound:null?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif1
    mov rax, PVAR(0);get param:ls
    jmp doneif1
    dif1:
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:ls
    push rax
    
    push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 40]
    push rax
    push qword 1
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody7)
		jmp doneclousure7
		lambody7:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:g
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*0
       mov rax, qword [rbx];get fvar:box
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov PVAR(0), rax;setting param:g
    mov rax, SOB_VOID_ADDRESS
		push SOB_NIL_ADDRESS
			EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody8)
		jmp doneclousure8
		lambody8:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:ls
    push rax
    push qword 1
    GET_BOUND rax,4,0;get bound:null?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif2
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			GET_BOUND rax,1,1;get bound:args
    push rax
    push qword 1
    GET_BOUND rax,4,2;get bound:cdr
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			GET_BOUND rax,1,1;get bound:args
    push rax
    push qword 1
    GET_BOUND rax,4,1;get bound:car
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    
    push SOB_NIL_ADDRESS
			GET_BOUND rax,2,0;get bound:f
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    jmp doneif2
    dif2:
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:ls
    push rax
    push qword 1
    GET_BOUND rax,4,2;get bound:cdr
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    push qword 1
    push SOB_NIL_ADDRESS
			GET_BOUND rax,0,0;get bound:g
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:ls
    push rax
    push qword 1
    GET_BOUND rax,4,1;get bound:car
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    
    GET_BOUND rax,4,3;get bound:cons
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    doneif2:
    leave
    ret
		doneclousure8:
    push rax
			mov rax, PVAR(0);get param:g
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*7
       mov rax, qword [rbx];get fvar:set-car!
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
		push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:g
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC;seq ret;seq ret
    leave
    ret
		doneclousure7:
    violation_check rax, T_CLOSURE
      REST_APPLIC
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 1
    REST_APPLICTP
    doneif1:
    leave
    ret
		doneclousure6:
    push rax
			mov rax, PVAR(0);get param:f
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*7
       mov rax, qword [rbx];get fvar:set-car!
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
		push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:f
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC;seq ret;seq ret
    leave
    ret
		doneclousure5:
    violation_check rax, T_CLOSURE
      REST_APPLIC
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    leave
    ret
		doneclousure4:
    leave
    ret
		doneclousure3:
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov qword [fvar_tbl + WORD_SIZE*49], rax;setting fvar:append
    mov rax, SOB_VOID_ADDRESS
		call write_sob_if_not_void

		
    push SOB_NIL_ADDRESS
			mov rbx, fvar_tbl + WORD_SIZE*3
       mov rax, qword [rbx];get fvar:apply
		IS_UNDEF rbx
    push rax
    push qword 1
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody9)
		jmp doneclousure9
		lambody9:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody10)
		jmp doneclousure10
		lambody10:
		push rbp
    mov rbp, rsp
    OPT_MAKE_LIST_IF_NEEDED 0
    push SOB_NIL_ADDRESS
    
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:x
    push rax
    push qword 1
    GET_BOUND rax,0,0;get bound:apply
    violation_check rax, T_CLOSURE
      REST_APPLIC
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 0
    REST_APPLICTP
    leave
    ret
		doneclousure10:
    leave
    ret
		doneclousure9:
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov qword [fvar_tbl + WORD_SIZE*3], rax;setting fvar:apply
    mov rax, SOB_VOID_ADDRESS
		call write_sob_if_not_void

		
    push SOB_NIL_ADDRESS
			mov rbx, fvar_tbl + WORD_SIZE*36
       mov rax, qword [rbx];get fvar:=
		IS_UNDEF rbx
    push rax
    push qword 1
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody11)
		jmp doneclousure11
		lambody11:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody12)
		jmp doneclousure12
		lambody12:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 48]
    push rax
			mov rax, PVAR(0);get param:x
    push rax
    
    GET_BOUND rax,0,0;get bound:=
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    leave
    ret
		doneclousure12:
    leave
    ret
		doneclousure11:
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov qword [fvar_tbl + WORD_SIZE*48], rax;setting fvar:zero?
    mov rax, SOB_VOID_ADDRESS
		call write_sob_if_not_void

		
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody13)
		jmp doneclousure13
		lambody13:
		push rbp
    mov rbp, rsp
    OPT_MAKE_LIST_IF_NEEDED 0
    mov rax, PVAR(0);get param:x
    leave
    ret
		doneclousure13:
    mov qword [fvar_tbl + WORD_SIZE*47], rax;setting fvar:list
    mov rax, SOB_VOID_ADDRESS
		call write_sob_if_not_void

		
    push SOB_NIL_ADDRESS
			mov rbx, fvar_tbl + WORD_SIZE*5
       mov rax, qword [rbx];get fvar:cdr
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*12
       mov rax, qword [rbx];get fvar:pair?
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*13
       mov rax, qword [rbx];get fvar:null?
		IS_UNDEF rbx
    push rax
    push qword 3
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody14)
		jmp doneclousure14
		lambody14:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 3
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody15)
		jmp doneclousure15
		lambody15:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:x
    push rax
    push qword 1
    GET_BOUND rax,0,0;get bound:null?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    jne doneor1
		push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:x
    push rax
    push qword 1
    GET_BOUND rax,0,1;get bound:pair?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif3
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:x
    push rax
    push qword 1
    GET_BOUND rax,0,2;get bound:cdr
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    
    mov rbx, fvar_tbl + WORD_SIZE*46
       mov rax, qword [rbx];get fvar:list?
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 1
    REST_APPLICTP
    jmp doneif3
    dif3:
    mov rax, [const_tbl + 16]
    doneif3:
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    jne doneor1
		mov rax, [const_tbl + 16]
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    jne doneor1    
    doneor1:
    leave
    ret
		doneclousure15:
    leave
    ret
		doneclousure14:
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov qword [fvar_tbl + WORD_SIZE*46], rax;setting fvar:list?
    mov rax, SOB_VOID_ADDRESS
		call write_sob_if_not_void

		
    push SOB_NIL_ADDRESS
			mov rbx, fvar_tbl + WORD_SIZE*31
       mov rax, qword [rbx];get fvar:+
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*5
       mov rax, qword [rbx];get fvar:cdr
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*12
       mov rax, qword [rbx];get fvar:pair?
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*13
       mov rax, qword [rbx];get fvar:null?
		IS_UNDEF rbx
    push rax
    push qword 4
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody16)
		jmp doneclousure16
		lambody16:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 4
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody17)
		jmp doneclousure17
		lambody17:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 40]
    push rax
			mov rax, [const_tbl + 40]
    push rax
    
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody18)
		jmp doneclousure18
		lambody18:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 2
    
    push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*0
       mov rax, qword [rbx];get fvar:box
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov PVAR(1), rax;setting param:loop
    mov rax, SOB_VOID_ADDRESS
		
    mov rax, [const_tbl + 48]
    mov PVAR(0), rax;setting param:count
    mov rax, SOB_VOID_ADDRESS
		push SOB_NIL_ADDRESS
			EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody19)
		jmp doneclousure19
		lambody19:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 2
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,2,0;get bound:null?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif4
    mov rax, PVAR(1);get param:count
    jmp doneif4
    dif4:
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,2,1;get bound:pair?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif5
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:count
    push rax
			mov rax, [const_tbl + 56]
    push rax
    push qword 2
    GET_BOUND rax,2,3;get bound:+
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,2,2;get bound:cdr
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    
    push SOB_NIL_ADDRESS
			GET_BOUND rax,0,1;get bound:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    jmp doneif5
    dif5:
    mov rax, [const_tbl + 64]
    doneif5:
    doneif4:
    leave
    ret
		doneclousure19:
    push rax
			mov rax, PVAR(1);get param:loop
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*7
       mov rax, qword [rbx];get fvar:set-car!
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
		push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 48]
    push rax
			GET_BOUND rax,0,0;get bound:x
    push rax
    
    push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP;seq ret;seq ret
    leave
    ret
		doneclousure18:
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    leave
    ret
		doneclousure17:
    leave
    ret
		doneclousure16:
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov qword [fvar_tbl + WORD_SIZE*40], rax;setting fvar:length
    mov rax, SOB_VOID_ADDRESS
		call write_sob_if_not_void

		
    push SOB_NIL_ADDRESS
			mov rbx, fvar_tbl + WORD_SIZE*40
       mov rax, qword [rbx];get fvar:length
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*36
       mov rax, qword [rbx];get fvar:=
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*13
       mov rax, qword [rbx];get fvar:null?
		IS_UNDEF rbx
    push rax
    push qword 4
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody20)
		jmp doneclousure20
		lambody20:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 4
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody21)
		jmp doneclousure21
		lambody21:
		push rbp
    mov rbp, rsp
    OPT_MAKE_LIST_IF_NEEDED 1
    push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:y
    push rax
    push qword 1
    GET_BOUND rax,0,0;get bound:null?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif6
    push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 72]
    push rax
			mov rax, PVAR(0);get param:x
    push rax
    
    mov rbx, fvar_tbl + WORD_SIZE*22
       mov rax, qword [rbx];get fvar:make-string
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    jmp doneif6
    dif6:
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:y
    push rax
    push qword 1
    GET_BOUND rax,0,3;get bound:length
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			mov rax, [const_tbl + 56]
    push rax
    push qword 2
    GET_BOUND rax,0,2;get bound:=
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif7
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:y
    push rax
    push qword 1
    GET_BOUND rax,0,1;get bound:car
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			mov rax, PVAR(0);get param:x
    push rax
    
    mov rbx, fvar_tbl + WORD_SIZE*22
       mov rax, qword [rbx];get fvar:make-string
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    jmp doneif7
    dif7:
    mov rax, [const_tbl + 64]
    doneif7:
    doneif6:
    leave
    ret
		doneclousure21:
    leave
    ret
		doneclousure20:
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov qword [fvar_tbl + WORD_SIZE*22], rax;setting fvar:make-string
    mov rax, SOB_VOID_ADDRESS
		call write_sob_if_not_void

		
    push SOB_NIL_ADDRESS
			mov rbx, fvar_tbl + WORD_SIZE*13
       mov rax, qword [rbx];get fvar:null?
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*40
       mov rax, qword [rbx];get fvar:length
		IS_UNDEF rbx
    push rax
    push qword 3
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody22)
		jmp doneclousure22
		lambody22:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 3
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody23)
		jmp doneclousure23
		lambody23:
		push rbp
    mov rbp, rsp
    OPT_MAKE_LIST_IF_NEEDED 1
    push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:y
    push rax
    push qword 1
    GET_BOUND rax,0,2;get bound:null?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif8
    push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 48]
    push rax
			mov rax, PVAR(0);get param:x
    push rax
    
    mov rbx, fvar_tbl + WORD_SIZE*26
       mov rax, qword [rbx];get fvar:make-vector
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    jmp doneif8
    dif8:
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:y
    push rax
    push qword 1
    GET_BOUND rax,0,0;get bound:length
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			mov rax, [const_tbl + 56]
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*36
       mov rax, qword [rbx];get fvar:=
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif9
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:y
    push rax
    push qword 1
    GET_BOUND rax,0,1;get bound:car
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			mov rax, PVAR(0);get param:x
    push rax
    
    mov rbx, fvar_tbl + WORD_SIZE*26
       mov rax, qword [rbx];get fvar:make-vector
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    jmp doneif9
    dif9:
    mov rax, [const_tbl + 64]
    doneif9:
    doneif8:
    leave
    ret
		doneclousure23:
    leave
    ret
		doneclousure22:
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov qword [fvar_tbl + WORD_SIZE*26], rax;setting fvar:make-vector
    mov rax, SOB_VOID_ADDRESS
		call write_sob_if_not_void

		
    push SOB_NIL_ADDRESS
			mov rbx, fvar_tbl + WORD_SIZE*30
       mov rax, qword [rbx];get fvar:eq?
		IS_UNDEF rbx
    push rax
    push qword 1
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody24)
		jmp doneclousure24
		lambody24:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody25)
		jmp doneclousure25
		lambody25:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 24]
    push rax
			mov rax, PVAR(0);get param:x
    push rax
    push qword 2
    GET_BOUND rax,0,0;get bound:eq?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif10
    mov rax, [const_tbl + 16]
    jmp doneif10
    dif10:
    mov rax, [const_tbl + 24]
    doneif10:
    leave
    ret
		doneclousure25:
    leave
    ret
		doneclousure24:
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov qword [fvar_tbl + WORD_SIZE*38], rax;setting fvar:not
    mov rax, SOB_VOID_ADDRESS
		call write_sob_if_not_void

		
    push SOB_NIL_ADDRESS
			mov rbx, fvar_tbl + WORD_SIZE*11
       mov rax, qword [rbx];get fvar:integer?
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*10
       mov rax, qword [rbx];get fvar:float?
		IS_UNDEF rbx
    push rax
    push qword 2
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody26)
		jmp doneclousure26
		lambody26:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 2
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody27)
		jmp doneclousure27
		lambody27:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:x
    push rax
    push qword 1
    GET_BOUND rax,0,0;get bound:float?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    jne doneor2
		push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:x
    push rax
    
    GET_BOUND rax,0,1;get bound:integer?
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 1
    REST_APPLICTP
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    jne doneor2
		mov rax, [const_tbl + 16]
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    jne doneor2    
    doneor2:
    leave
    ret
		doneclousure27:
    leave
    ret
		doneclousure26:
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov qword [fvar_tbl + WORD_SIZE*45], rax;setting fvar:number?
    mov rax, SOB_VOID_ADDRESS
		call write_sob_if_not_void

		
    push SOB_NIL_ADDRESS
			mov rbx, fvar_tbl + WORD_SIZE*5
       mov rax, qword [rbx];get fvar:cdr
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*3
       mov rax, qword [rbx];get fvar:apply
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*6
       mov rax, qword [rbx];get fvar:cons
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*13
       mov rax, qword [rbx];get fvar:null?
		IS_UNDEF rbx
    push rax
    push qword 5
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody28)
		jmp doneclousure28
		lambody28:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 5
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody29)
		jmp doneclousure29
		lambody29:
		push rbp
    mov rbp, rsp
    OPT_MAKE_LIST_IF_NEEDED 2
    push SOB_NIL_ADDRESS
			mov rax, PVAR(2);get param:more
    push rax
    push qword 1
    GET_BOUND rax,0,0;get bound:null?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif11
    push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:ls
    push rax
    
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody33)
		jmp doneclousure33
		lambody33:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 40]
    push rax
    
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody34)
		jmp doneclousure34
		lambody34:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:map1
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*0
       mov rax, qword [rbx];get fvar:box
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov PVAR(0), rax;setting param:map1
    mov rax, SOB_VOID_ADDRESS
		push SOB_NIL_ADDRESS
			EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody35)
		jmp doneclousure35
		lambody35:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:ls
    push rax
    push qword 1
    GET_BOUND rax,3,0;get bound:null?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif13
    mov rax, [const_tbl + 8]
    jmp doneif13
    dif13:
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:ls
    push rax
    push qword 1
    GET_BOUND rax,3,4;get bound:cdr
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    push qword 1
    push SOB_NIL_ADDRESS
			GET_BOUND rax,0,0;get bound:map1
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:ls
    push rax
    push qword 1
    GET_BOUND rax,3,3;get bound:car
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    push qword 1
    GET_BOUND rax,2,0;get bound:f
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    
    GET_BOUND rax,3,1;get bound:cons
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    doneif13:
    leave
    ret
		doneclousure35:
    push rax
			mov rax, PVAR(0);get param:map1
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*7
       mov rax, qword [rbx];get fvar:set-car!
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
		push SOB_NIL_ADDRESS
			GET_BOUND rax,0,0;get bound:ls
    push rax
    
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:map1
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 1
    REST_APPLICTP;seq ret;seq ret
    leave
    ret
		doneclousure34:
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 1
    REST_APPLICTP
    leave
    ret
		doneclousure33:
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 1
    REST_APPLICTP
    jmp doneif11
    dif11:
    push SOB_NIL_ADDRESS
			mov rax, PVAR(2);get param:more
    push rax
			mov rax, PVAR(1);get param:ls
    push rax
    
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody30)
		jmp doneclousure30
		lambody30:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 2
    push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 40]
    push rax
    
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody31)
		jmp doneclousure31
		lambody31:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:map-more
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*0
       mov rax, qword [rbx];get fvar:box
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov PVAR(0), rax;setting param:map-more
    mov rax, SOB_VOID_ADDRESS
		push SOB_NIL_ADDRESS
			EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody32)
		jmp doneclousure32
		lambody32:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 2
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:ls
    push rax
    push qword 1
    GET_BOUND rax,3,0;get bound:null?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif12
    mov rax, [const_tbl + 8]
    jmp doneif12
    dif12:
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:more
    push rax
			GET_BOUND rax,3,4;get bound:cdr
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*44
       mov rax, qword [rbx];get fvar:map
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:ls
    push rax
    push qword 1
    GET_BOUND rax,3,4;get bound:cdr
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    push qword 2
    push SOB_NIL_ADDRESS
			GET_BOUND rax,0,0;get bound:map-more
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:more
    push rax
			GET_BOUND rax,3,3;get bound:car
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*44
       mov rax, qword [rbx];get fvar:map
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:ls
    push rax
    push qword 1
    GET_BOUND rax,3,3;get bound:car
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			GET_BOUND rax,2,0;get bound:f
    push rax
    push qword 3
    GET_BOUND rax,3,2;get bound:apply
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    
    GET_BOUND rax,3,1;get bound:cons
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    doneif12:
    leave
    ret
		doneclousure32:
    push rax
			mov rax, PVAR(0);get param:map-more
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*7
       mov rax, qword [rbx];get fvar:set-car!
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
		push SOB_NIL_ADDRESS
			GET_BOUND rax,0,1;get bound:more
    push rax
			GET_BOUND rax,0,0;get bound:ls
    push rax
    
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:map-more
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP;seq ret;seq ret
    leave
    ret
		doneclousure31:
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 1
    REST_APPLICTP
    leave
    ret
		doneclousure30:
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    doneif11:
    leave
    ret
		doneclousure29:
    leave
    ret
		doneclousure28:
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov qword [fvar_tbl + WORD_SIZE*44], rax;setting fvar:map
    mov rax, SOB_VOID_ADDRESS
		call write_sob_if_not_void

		
    push SOB_NIL_ADDRESS
			mov rbx, fvar_tbl + WORD_SIZE*31
       mov rax, qword [rbx];get fvar:+
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*40
       mov rax, qword [rbx];get fvar:length
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*26
       mov rax, qword [rbx];get fvar:make-vector
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*5
       mov rax, qword [rbx];get fvar:cdr
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*12
       mov rax, qword [rbx];get fvar:pair?
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*13
       mov rax, qword [rbx];get fvar:null?
		IS_UNDEF rbx
    push rax
    push qword 7
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody36)
		jmp doneclousure36
		lambody36:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 7
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody37)
		jmp doneclousure37
		lambody37:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 40]
    push rax
    
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody38)
		jmp doneclousure38
		lambody38:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*0
       mov rax, qword [rbx];get fvar:box
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov PVAR(0), rax;setting param:loop
    mov rax, SOB_VOID_ADDRESS
		push SOB_NIL_ADDRESS
			EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody39)
		jmp doneclousure39
		lambody39:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 3
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,2,0;get bound:null?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif14
    mov rax, PVAR(1);get param:vec
    jmp doneif14
    dif14:
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,2,1;get bound:pair?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif15
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(2);get param:count
    push rax
			mov rax, [const_tbl + 56]
    push rax
    push qword 2
    GET_BOUND rax,2,6;get bound:+
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,2,2;get bound:car
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			mov rax, PVAR(2);get param:count
    push rax
			mov rax, PVAR(1);get param:vec
    push rax
    push qword 3
    mov rbx, fvar_tbl + WORD_SIZE*25
       mov rax, qword [rbx];get fvar:vector-set!
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
		mov rax, PVAR(1);get param:vec;seq ret
    push rax
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,2,3;get bound:cdr
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    
    push SOB_NIL_ADDRESS
			GET_BOUND rax,0,0;get bound:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 3
    REST_APPLICTP
    jmp doneif15
    dif15:
    mov rax, [const_tbl + 64]
    doneif15:
    doneif14:
    leave
    ret
		doneclousure39:
    push rax
			mov rax, PVAR(0);get param:loop
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*7
       mov rax, qword [rbx];get fvar:set-car!
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
		push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 48]
    push rax
			push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			GET_BOUND rax,0,0;get bound:lst
    push rax
    push qword 1
    GET_BOUND rax,1,5;get bound:length
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    push qword 1
    GET_BOUND rax,1,4;get bound:make-vector
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			GET_BOUND rax,0,0;get bound:lst
    push rax
    
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 3
    REST_APPLICTP;seq ret;seq ret
    leave
    ret
		doneclousure38:
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 1
    REST_APPLICTP
    leave
    ret
		doneclousure37:
    leave
    ret
		doneclousure36:
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov qword [fvar_tbl + WORD_SIZE*42], rax;setting fvar:list->vector
    mov rax, SOB_VOID_ADDRESS
		call write_sob_if_not_void

		
    push SOB_NIL_ADDRESS
			mov rbx, fvar_tbl + WORD_SIZE*33
       mov rax, qword [rbx];get fvar:-
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*23
       mov rax, qword [rbx];get fvar:vector-length
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*6
       mov rax, qword [rbx];get fvar:cons
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*24
       mov rax, qword [rbx];get fvar:vector-ref
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*35
       mov rax, qword [rbx];get fvar:<
		IS_UNDEF rbx
    push rax
    push qword 5
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody40)
		jmp doneclousure40
		lambody40:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 5
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody41)
		jmp doneclousure41
		lambody41:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 40]
    push rax
    
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody42)
		jmp doneclousure42
		lambody42:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*0
       mov rax, qword [rbx];get fvar:box
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov PVAR(0), rax;setting param:loop
    mov rax, SOB_VOID_ADDRESS
		push SOB_NIL_ADDRESS
			EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody43)
		jmp doneclousure43
		lambody43:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 3
    push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 48]
    push rax
			mov rax, PVAR(2);get param:count
    push rax
    push qword 2
    GET_BOUND rax,2,0;get bound:<
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif16
    mov rax, PVAR(1);get param:lst
    jmp doneif16
    dif16:
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 56]
    push rax
			mov rax, PVAR(2);get param:count
    push rax
    push qword 2
    GET_BOUND rax,2,4;get bound:-
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:lst
    push rax
			push SOB_NIL_ADDRESS
			mov rax, PVAR(2);get param:count
    push rax
			mov rax, PVAR(0);get param:vec
    push rax
    push qword 2
    GET_BOUND rax,2,1;get bound:vector-ref
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    push qword 2
    GET_BOUND rax,2,2;get bound:cons
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			mov rax, PVAR(0);get param:vec
    push rax
    
    push SOB_NIL_ADDRESS
			GET_BOUND rax,0,0;get bound:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 3
    REST_APPLICTP
    doneif16:
    leave
    ret
		doneclousure43:
    push rax
			mov rax, PVAR(0);get param:loop
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*7
       mov rax, qword [rbx];get fvar:set-car!
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
		push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 56]
    push rax
			push SOB_NIL_ADDRESS
			GET_BOUND rax,0,0;get bound:vec
    push rax
    push qword 1
    GET_BOUND rax,1,3;get bound:vector-length
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    push qword 2
    GET_BOUND rax,1,4;get bound:-
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			mov rax, [const_tbl + 8]
    push rax
			GET_BOUND rax,0,0;get bound:vec
    push rax
    
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 3
    REST_APPLICTP;seq ret;seq ret
    leave
    ret
		doneclousure42:
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 1
    REST_APPLICTP
    leave
    ret
		doneclousure41:
    leave
    ret
		doneclousure40:
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov qword [fvar_tbl + WORD_SIZE*43], rax;setting fvar:vector->list
    mov rax, SOB_VOID_ADDRESS
		call write_sob_if_not_void

		
    push SOB_NIL_ADDRESS
			mov rbx, fvar_tbl + WORD_SIZE*42
       mov rax, qword [rbx];get fvar:list->vector
		IS_UNDEF rbx
    push rax
    push qword 1
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody44)
		jmp doneclousure44
		lambody44:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody45)
		jmp doneclousure45
		lambody45:
		push rbp
    mov rbp, rsp
    OPT_MAKE_LIST_IF_NEEDED 0
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:x
    push rax
    
    GET_BOUND rax,0,0;get bound:list->vector
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 1
    REST_APPLICTP
    leave
    ret
		doneclousure45:
    leave
    ret
		doneclousure44:
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov qword [fvar_tbl + WORD_SIZE*41], rax;setting fvar:vector
    mov rax, SOB_VOID_ADDRESS
		call write_sob_if_not_void

		
    push SOB_NIL_ADDRESS
			mov rbx, fvar_tbl + WORD_SIZE*5
       mov rax, qword [rbx];get fvar:cdr
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*3
       mov rax, qword [rbx];get fvar:apply
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*31
       mov rax, qword [rbx];get fvar:+
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*13
       mov rax, qword [rbx];get fvar:null?
		IS_UNDEF rbx
    push rax
    push qword 5
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody46)
		jmp doneclousure46
		lambody46:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 5
    push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 40]
    push rax
    
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody47)
		jmp doneclousure47
		lambody47:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*0
       mov rax, qword [rbx];get fvar:box
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov PVAR(0), rax;setting param:loop
    mov rax, SOB_VOID_ADDRESS
		push SOB_NIL_ADDRESS
			EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody48)
		jmp doneclousure48
		lambody48:
		push rbp
    mov rbp, rsp
    OPT_MAKE_LIST_IF_NEEDED 0
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:x
    push rax
    push qword 1
    GET_BOUND rax,1,0;get bound:null?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif17
    mov rax, [const_tbl + 48]
    jmp doneif17
    dif17:
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:x
    push rax
    push qword 1
    GET_BOUND rax,1,4;get bound:cdr
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			GET_BOUND rax,0,0;get bound:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    push qword 2
    GET_BOUND rax,1,3;get bound:apply
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:x
    push rax
    push qword 1
    GET_BOUND rax,1,2;get bound:car
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    
    GET_BOUND rax,1,1;get bound:+
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    doneif17:
    leave
    ret
		doneclousure48:
    push rax
			mov rax, PVAR(0);get param:loop
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*7
       mov rax, qword [rbx];get fvar:set-car!
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
		push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC;seq ret;seq ret
    leave
    ret
		doneclousure47:
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 1
    REST_APPLICTP
    leave
    ret
		doneclousure46:
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov qword [fvar_tbl + WORD_SIZE*31], rax;setting fvar:+
    mov rax, SOB_VOID_ADDRESS
		call write_sob_if_not_void

		
    push SOB_NIL_ADDRESS
			mov rbx, fvar_tbl + WORD_SIZE*5
       mov rax, qword [rbx];get fvar:cdr
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*3
       mov rax, qword [rbx];get fvar:apply
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*32
       mov rax, qword [rbx];get fvar:*
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*13
       mov rax, qword [rbx];get fvar:null?
		IS_UNDEF rbx
    push rax
    push qword 5
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody49)
		jmp doneclousure49
		lambody49:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 5
    push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 40]
    push rax
    
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody50)
		jmp doneclousure50
		lambody50:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*0
       mov rax, qword [rbx];get fvar:box
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov PVAR(0), rax;setting param:loop
    mov rax, SOB_VOID_ADDRESS
		push SOB_NIL_ADDRESS
			EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody51)
		jmp doneclousure51
		lambody51:
		push rbp
    mov rbp, rsp
    OPT_MAKE_LIST_IF_NEEDED 0
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:x
    push rax
    push qword 1
    GET_BOUND rax,1,0;get bound:null?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif18
    mov rax, [const_tbl + 56]
    jmp doneif18
    dif18:
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:x
    push rax
    push qword 1
    GET_BOUND rax,1,4;get bound:cdr
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			GET_BOUND rax,0,0;get bound:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    push qword 2
    GET_BOUND rax,1,3;get bound:apply
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:x
    push rax
    push qword 1
    GET_BOUND rax,1,2;get bound:car
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    
    GET_BOUND rax,1,1;get bound:*
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    doneif18:
    leave
    ret
		doneclousure51:
    push rax
			mov rax, PVAR(0);get param:loop
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*7
       mov rax, qword [rbx];get fvar:set-car!
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
		push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC;seq ret;seq ret
    leave
    ret
		doneclousure50:
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 1
    REST_APPLICTP
    leave
    ret
		doneclousure49:
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov qword [fvar_tbl + WORD_SIZE*32], rax;setting fvar:*
    mov rax, SOB_VOID_ADDRESS
		call write_sob_if_not_void

		
    push SOB_NIL_ADDRESS
			mov rbx, fvar_tbl + WORD_SIZE*5
       mov rax, qword [rbx];get fvar:cdr
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*40
       mov rax, qword [rbx];get fvar:length
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*3
       mov rax, qword [rbx];get fvar:apply
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*31
       mov rax, qword [rbx];get fvar:+
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*33
       mov rax, qword [rbx];get fvar:-
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*13
       mov rax, qword [rbx];get fvar:null?
		IS_UNDEF rbx
    push rax
    push qword 7
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody52)
		jmp doneclousure52
		lambody52:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 7
    push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 40]
    push rax
    
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody53)
		jmp doneclousure53
		lambody53:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*0
       mov rax, qword [rbx];get fvar:box
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov PVAR(0), rax;setting param:loop
    mov rax, SOB_VOID_ADDRESS
		push SOB_NIL_ADDRESS
			EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody54)
		jmp doneclousure54
		lambody54:
		push rbp
    mov rbp, rsp
    OPT_MAKE_LIST_IF_NEEDED 0
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:x
    push rax
    push qword 1
    GET_BOUND rax,1,0;get bound:null?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif19
    mov rax, [const_tbl + 48]
    jmp doneif19
    dif19:
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:x
    push rax
    push qword 1
    GET_BOUND rax,1,3;get bound:car
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:x
    push rax
    push qword 1
    GET_BOUND rax,1,6;get bound:cdr
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			GET_BOUND rax,0,0;get bound:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    push qword 2
    GET_BOUND rax,1,4;get bound:apply
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    
    GET_BOUND rax,1,1;get bound:-
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    doneif19:
    leave
    ret
		doneclousure54:
    push rax
			mov rax, PVAR(0);get param:loop
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*7
       mov rax, qword [rbx];get fvar:set-car!
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
		EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody55)
		jmp doneclousure55
		lambody55:
		push rbp
    mov rbp, rsp
    OPT_MAKE_LIST_IF_NEEDED 0
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:num
    push rax
    push qword 1
    GET_BOUND rax,1,0;get bound:null?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif20
    mov rax, [const_tbl + 64]
    jmp doneif20
    dif20:
    push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 56]
    push rax
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:num
    push rax
    push qword 1
    GET_BOUND rax,1,5;get bound:length
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*36
       mov rax, qword [rbx];get fvar:=
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif21
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:num
    push rax
    push qword 1
    GET_BOUND rax,1,3;get bound:car
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			mov rax, [const_tbl + 48]
    push rax
    
    GET_BOUND rax,1,1;get bound:-
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    jmp doneif21
    dif21:
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:num
    push rax
    push qword 1
    GET_BOUND rax,1,6;get bound:cdr
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			GET_BOUND rax,0,0;get bound:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    push qword 2
    GET_BOUND rax,1,4;get bound:apply
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:num
    push rax
    push qword 1
    GET_BOUND rax,1,3;get bound:car
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    
    GET_BOUND rax,1,2;get bound:+
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    doneif21:
    doneif20:
    leave
    ret
		doneclousure55:;seq ret;seq ret
    leave
    ret
		doneclousure53:
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 1
    REST_APPLICTP
    leave
    ret
		doneclousure52:
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov qword [fvar_tbl + WORD_SIZE*33], rax;setting fvar:-
    mov rax, SOB_VOID_ADDRESS
		call write_sob_if_not_void

		
    push SOB_NIL_ADDRESS
			mov rbx, fvar_tbl + WORD_SIZE*5
       mov rax, qword [rbx];get fvar:cdr
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*40
       mov rax, qword [rbx];get fvar:length
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*3
       mov rax, qword [rbx];get fvar:apply
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*32
       mov rax, qword [rbx];get fvar:*
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*34
       mov rax, qword [rbx];get fvar:/
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*13
       mov rax, qword [rbx];get fvar:null?
		IS_UNDEF rbx
    push rax
    push qword 7
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody56)
		jmp doneclousure56
		lambody56:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 7
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody57)
		jmp doneclousure57
		lambody57:
		push rbp
    mov rbp, rsp
    OPT_MAKE_LIST_IF_NEEDED 0
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:num
    push rax
    push qword 1
    GET_BOUND rax,0,0;get bound:null?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif22
    mov rax, [const_tbl + 64]
    jmp doneif22
    dif22:
    push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 56]
    push rax
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:num
    push rax
    push qword 1
    GET_BOUND rax,0,5;get bound:length
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*36
       mov rax, qword [rbx];get fvar:=
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif23
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:num
    push rax
    push qword 1
    GET_BOUND rax,0,3;get bound:car
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			mov rax, [const_tbl + 56]
    push rax
    
    GET_BOUND rax,0,1;get bound:/
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    jmp doneif23
    dif23:
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:num
    push rax
    push qword 1
    GET_BOUND rax,0,6;get bound:cdr
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			GET_BOUND rax,0,2;get bound:*
    push rax
    push qword 2
    GET_BOUND rax,0,4;get bound:apply
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:num
    push rax
    push qword 1
    GET_BOUND rax,0,3;get bound:car
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    
    GET_BOUND rax,0,1;get bound:/
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    doneif23:
    doneif22:
    leave
    ret
		doneclousure57:
    leave
    ret
		doneclousure56:
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov qword [fvar_tbl + WORD_SIZE*34], rax;setting fvar:/
    mov rax, SOB_VOID_ADDRESS
		call write_sob_if_not_void

		
    push SOB_NIL_ADDRESS
			mov rbx, fvar_tbl + WORD_SIZE*5
       mov rax, qword [rbx];get fvar:cdr
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*36
       mov rax, qword [rbx];get fvar:=
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*13
       mov rax, qword [rbx];get fvar:null?
		IS_UNDEF rbx
    push rax
    push qword 4
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody58)
		jmp doneclousure58
		lambody58:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 4
    push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 40]
    push rax
    
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody59)
		jmp doneclousure59
		lambody59:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*0
       mov rax, qword [rbx];get fvar:box
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov PVAR(0), rax;setting param:loop
    mov rax, SOB_VOID_ADDRESS
		push SOB_NIL_ADDRESS
			EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody60)
		jmp doneclousure60
		lambody60:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 2
    push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,1,0;get bound:null?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif24
    mov rax, [const_tbl + 24]
    jmp doneif24
    dif24:
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,1,2;get bound:car
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			mov rax, PVAR(0);get param:element
    push rax
    push qword 2
    GET_BOUND rax,1,1;get bound:=
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif25
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,1,3;get bound:cdr
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,1,2;get bound:car
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    
    push SOB_NIL_ADDRESS
			GET_BOUND rax,0,0;get bound:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    jmp doneif25
    dif25:
    mov rax, [const_tbl + 16]
    doneif25:
    doneif24:
    leave
    ret
		doneclousure60:
    push rax
			mov rax, PVAR(0);get param:loop
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*7
       mov rax, qword [rbx];get fvar:set-car!
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
		EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody61)
		jmp doneclousure61
		lambody61:
		push rbp
    mov rbp, rsp
    OPT_MAKE_LIST_IF_NEEDED 0
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,1,0;get bound:null?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif26
    mov rax, [const_tbl + 64]
    jmp doneif26
    dif26:
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,1,3;get bound:cdr
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,1,2;get bound:car
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    
    push SOB_NIL_ADDRESS
			GET_BOUND rax,0,0;get bound:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    doneif26:
    leave
    ret
		doneclousure61:;seq ret;seq ret
    leave
    ret
		doneclousure59:
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 1
    REST_APPLICTP
    leave
    ret
		doneclousure58:
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov qword [fvar_tbl + WORD_SIZE*36], rax;setting fvar:=
    mov rax, SOB_VOID_ADDRESS
		call write_sob_if_not_void

		
    push SOB_NIL_ADDRESS
			mov rbx, fvar_tbl + WORD_SIZE*5
       mov rax, qword [rbx];get fvar:cdr
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*35
       mov rax, qword [rbx];get fvar:<
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*13
       mov rax, qword [rbx];get fvar:null?
		IS_UNDEF rbx
    push rax
    push qword 4
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody62)
		jmp doneclousure62
		lambody62:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 4
    push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 40]
    push rax
    
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody63)
		jmp doneclousure63
		lambody63:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*0
       mov rax, qword [rbx];get fvar:box
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov PVAR(0), rax;setting param:loop
    mov rax, SOB_VOID_ADDRESS
		push SOB_NIL_ADDRESS
			EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody64)
		jmp doneclousure64
		lambody64:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 2
    push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,1,0;get bound:null?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif27
    mov rax, [const_tbl + 24]
    jmp doneif27
    dif27:
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,1,2;get bound:car
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			mov rax, PVAR(0);get param:element
    push rax
    push qword 2
    GET_BOUND rax,1,1;get bound:<
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif28
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,1,3;get bound:cdr
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,1,2;get bound:car
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    
    push SOB_NIL_ADDRESS
			GET_BOUND rax,0,0;get bound:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    jmp doneif28
    dif28:
    mov rax, [const_tbl + 16]
    doneif28:
    doneif27:
    leave
    ret
		doneclousure64:
    push rax
			mov rax, PVAR(0);get param:loop
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*7
       mov rax, qword [rbx];get fvar:set-car!
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
		EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody65)
		jmp doneclousure65
		lambody65:
		push rbp
    mov rbp, rsp
    OPT_MAKE_LIST_IF_NEEDED 0
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,1,0;get bound:null?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif29
    mov rax, [const_tbl + 64]
    jmp doneif29
    dif29:
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,1,3;get bound:cdr
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,1,2;get bound:car
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    
    push SOB_NIL_ADDRESS
			GET_BOUND rax,0,0;get bound:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    doneif29:
    leave
    ret
		doneclousure65:;seq ret;seq ret
    leave
    ret
		doneclousure63:
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 1
    REST_APPLICTP
    leave
    ret
		doneclousure62:
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov qword [fvar_tbl + WORD_SIZE*35], rax;setting fvar:<
    mov rax, SOB_VOID_ADDRESS
		call write_sob_if_not_void

		
    push SOB_NIL_ADDRESS
			mov rbx, fvar_tbl + WORD_SIZE*5
       mov rax, qword [rbx];get fvar:cdr
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*38
       mov rax, qword [rbx];get fvar:not
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*36
       mov rax, qword [rbx];get fvar:=
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*35
       mov rax, qword [rbx];get fvar:<
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*13
       mov rax, qword [rbx];get fvar:null?
		IS_UNDEF rbx
    push rax
    push qword 6
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody66)
		jmp doneclousure66
		lambody66:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 6
    push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 40]
    push rax
    
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody67)
		jmp doneclousure67
		lambody67:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*0
       mov rax, qword [rbx];get fvar:box
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov PVAR(0), rax;setting param:loop
    mov rax, SOB_VOID_ADDRESS
		push SOB_NIL_ADDRESS
			EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody68)
		jmp doneclousure68
		lambody68:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 2
    push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,1,0;get bound:null?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif30
    mov rax, [const_tbl + 24]
    jmp doneif30
    dif30:
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,1,4;get bound:car
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			mov rax, PVAR(0);get param:element
    push rax
    push qword 2
    GET_BOUND rax,1,1;get bound:<
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    jne doneor3
		push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,1,4;get bound:car
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			mov rax, PVAR(0);get param:element
    push rax
    push qword 2
    GET_BOUND rax,1,2;get bound:=
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    jne doneor3
		mov rax, [const_tbl + 16]
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    jne doneor3    
    doneor3:
    push rax
    push qword 1
    GET_BOUND rax,1,3;get bound:not
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif31
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,1,5;get bound:cdr
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,1,4;get bound:car
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    
    push SOB_NIL_ADDRESS
			GET_BOUND rax,0,0;get bound:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    jmp doneif31
    dif31:
    mov rax, [const_tbl + 16]
    doneif31:
    doneif30:
    leave
    ret
		doneclousure68:
    push rax
			mov rax, PVAR(0);get param:loop
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*7
       mov rax, qword [rbx];get fvar:set-car!
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
		EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody69)
		jmp doneclousure69
		lambody69:
		push rbp
    mov rbp, rsp
    OPT_MAKE_LIST_IF_NEEDED 0
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,1,0;get bound:null?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif32
    mov rax, [const_tbl + 64]
    jmp doneif32
    dif32:
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,1,5;get bound:cdr
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:lst
    push rax
    push qword 1
    GET_BOUND rax,1,4;get bound:car
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    
    push SOB_NIL_ADDRESS
			GET_BOUND rax,0,0;get bound:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    doneif32:
    leave
    ret
		doneclousure69:;seq ret;seq ret
    leave
    ret
		doneclousure67:
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 1
    REST_APPLICTP
    leave
    ret
		doneclousure66:
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov qword [fvar_tbl + WORD_SIZE*39], rax;setting fvar:>
    mov rax, SOB_VOID_ADDRESS
		call write_sob_if_not_void

		
    push SOB_NIL_ADDRESS
			mov rbx, fvar_tbl + WORD_SIZE*33
       mov rax, qword [rbx];get fvar:-
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*28
       mov rax, qword [rbx];get fvar:char->integer
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*5
       mov rax, qword [rbx];get fvar:cdr
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*30
       mov rax, qword [rbx];get fvar:eq?
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*15
       mov rax, qword [rbx];get fvar:vector?
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*16
       mov rax, qword [rbx];get fvar:string?
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*14
       mov rax, qword [rbx];get fvar:char?
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*12
       mov rax, qword [rbx];get fvar:pair?
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*10
       mov rax, qword [rbx];get fvar:float?
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*11
       mov rax, qword [rbx];get fvar:integer?
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*23
       mov rax, qword [rbx];get fvar:vector-length
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*24
       mov rax, qword [rbx];get fvar:vector-ref
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*20
       mov rax, qword [rbx];get fvar:string-ref
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*19
       mov rax, qword [rbx];get fvar:string-length
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*38
       mov rax, qword [rbx];get fvar:not
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*36
       mov rax, qword [rbx];get fvar:=
		IS_UNDEF rbx
    push rax
			mov rbx, fvar_tbl + WORD_SIZE*35
       mov rax, qword [rbx];get fvar:<
		IS_UNDEF rbx
    push rax
    push qword 18
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody70)
		jmp doneclousure70
		lambody70:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 18
    push SOB_NIL_ADDRESS
			EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody73)
		jmp doneclousure73
		lambody73:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 4
    push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 40]
    push rax
    
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody74)
		jmp doneclousure74
		lambody74:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*0
       mov rax, qword [rbx];get fvar:box
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov PVAR(0), rax;setting param:loop
    mov rax, SOB_VOID_ADDRESS
		push SOB_NIL_ADDRESS
			EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody75)
		jmp doneclousure75
		lambody75:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 4
    push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 48]
    push rax
			mov rax, PVAR(3);get param:index
    push rax
    push qword 2
    GET_BOUND rax,2,0;get bound:<
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif46
    mov rax, [const_tbl + 24]
    jmp doneif46
    dif46:
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(3);get param:index
    push rax
			mov rax, PVAR(1);get param:container-2
    push rax
    push qword 2
    mov rax, PVAR(2);get param:container-ref-fun
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			mov rax, PVAR(3);get param:index
    push rax
			mov rax, PVAR(0);get param:container-1
    push rax
    push qword 2
    mov rax, PVAR(2);get param:container-ref-fun
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*37
       mov rax, qword [rbx];get fvar:equal?
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif47
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 56]
    push rax
			mov rax, PVAR(3);get param:index
    push rax
    push qword 2
    GET_BOUND rax,2,17;get bound:-
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			mov rax, PVAR(2);get param:container-ref-fun
    push rax
			mov rax, PVAR(1);get param:container-2
    push rax
			mov rax, PVAR(0);get param:container-1
    push rax
    
    push SOB_NIL_ADDRESS
			GET_BOUND rax,0,0;get bound:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 4
    REST_APPLICTP
    jmp doneif47
    dif47:
    mov rax, [const_tbl + 16]
    doneif47:
    doneif46:
    leave
    ret
		doneclousure75:
    push rax
			mov rax, PVAR(0);get param:loop
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*7
       mov rax, qword [rbx];get fvar:set-car!
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
		push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			GET_BOUND rax,0,1;get bound:container-2
    push rax
    push qword 1
    GET_BOUND rax,0,3;get bound:container-size-fun
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			GET_BOUND rax,0,0;get bound:container-1
    push rax
    push qword 1
    GET_BOUND rax,0,3;get bound:container-size-fun
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    push qword 2
    GET_BOUND rax,1,1;get bound:=
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    push qword 1
    GET_BOUND rax,1,2;get bound:not
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif48
    mov rax, [const_tbl + 16]
    jmp doneif48
    dif48:
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 56]
    push rax
			push SOB_NIL_ADDRESS
			GET_BOUND rax,0,0;get bound:container-1
    push rax
    push qword 1
    GET_BOUND rax,0,3;get bound:container-size-fun
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    push qword 2
    GET_BOUND rax,1,17;get bound:-
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			GET_BOUND rax,0,2;get bound:container-ref-fun
    push rax
			GET_BOUND rax,0,1;get bound:container-2
    push rax
			GET_BOUND rax,0,0;get bound:container-1
    push rax
    
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:loop
    push rax
    push qword 1
    mov rbx, fvar_tbl + WORD_SIZE*4
       mov rax, qword [rbx];get fvar:car
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 4
    REST_APPLICTP
    doneif48:;seq ret;seq ret
    leave
    ret
		doneclousure74:
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 1
    REST_APPLICTP
    leave
    ret
		doneclousure73:
    push rax
    
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody71)
		jmp doneclousure71
		lambody71:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody72)
		jmp doneclousure72
		lambody72:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 2
    push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:x
    push rax
    push qword 1
    GET_BOUND rax,1,7;get bound:integer?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif33
    push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:y
    push rax
    push qword 1
    GET_BOUND rax,1,7;get bound:integer?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif34
    push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:y
    push rax
			mov rax, PVAR(0);get param:x
    push rax
    push qword 2
    GET_BOUND rax,1,1;get bound:=
    violation_check rax, T_CLOSURE
      REST_APPLIC
    jmp doneif34
    dif34:
    mov rax, [const_tbl + 16]
    doneif34:
    jmp doneif33
    dif33:
    mov rax, [const_tbl + 16]
    doneif33:
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    jne doneor4
		push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:x
    push rax
    push qword 1
    GET_BOUND rax,1,8;get bound:float?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif35
    push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:y
    push rax
    push qword 1
    GET_BOUND rax,1,8;get bound:float?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif36
    push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:y
    push rax
			mov rax, PVAR(0);get param:x
    push rax
    push qword 2
    GET_BOUND rax,1,1;get bound:=
    violation_check rax, T_CLOSURE
      REST_APPLIC
    jmp doneif36
    dif36:
    mov rax, [const_tbl + 16]
    doneif36:
    jmp doneif35
    dif35:
    mov rax, [const_tbl + 16]
    doneif35:
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    jne doneor4
		push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:x
    push rax
    push qword 1
    GET_BOUND rax,1,9;get bound:pair?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif37
    push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:y
    push rax
    push qword 1
    GET_BOUND rax,1,9;get bound:pair?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif38
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:y
    push rax
    push qword 1
    GET_BOUND rax,1,14;get bound:car
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:x
    push rax
    push qword 1
    GET_BOUND rax,1,14;get bound:car
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*37
       mov rax, qword [rbx];get fvar:equal?
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif39
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:y
    push rax
    push qword 1
    GET_BOUND rax,1,15;get bound:cdr
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:x
    push rax
    push qword 1
    GET_BOUND rax,1,15;get bound:cdr
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*37
       mov rax, qword [rbx];get fvar:equal?
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    jmp doneif39
    dif39:
    mov rax, [const_tbl + 16]
    doneif39:
    jmp doneif38
    dif38:
    mov rax, [const_tbl + 16]
    doneif38:
    jmp doneif37
    dif37:
    mov rax, [const_tbl + 16]
    doneif37:
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    jne doneor4
		push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:x
    push rax
    push qword 1
    GET_BOUND rax,1,10;get bound:char?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif40
    push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:y
    push rax
    push qword 1
    GET_BOUND rax,1,10;get bound:char?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif41
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:y
    push rax
    push qword 1
    GET_BOUND rax,1,16;get bound:char->integer
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:x
    push rax
    push qword 1
    GET_BOUND rax,1,16;get bound:char->integer
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    push qword 2
    GET_BOUND rax,1,1;get bound:=
    violation_check rax, T_CLOSURE
      REST_APPLIC
    jmp doneif41
    dif41:
    mov rax, [const_tbl + 16]
    doneif41:
    jmp doneif40
    dif40:
    mov rax, [const_tbl + 16]
    doneif40:
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    jne doneor4
		push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:x
    push rax
    push qword 1
    GET_BOUND rax,1,11;get bound:string?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif42
    push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:y
    push rax
    push qword 1
    GET_BOUND rax,1,11;get bound:string?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif43
    push SOB_NIL_ADDRESS
			GET_BOUND rax,1,3;get bound:string-length
    push rax
			GET_BOUND rax,1,4;get bound:string-ref
    push rax
			mov rax, PVAR(1);get param:y
    push rax
			mov rax, PVAR(0);get param:x
    push rax
    push qword 4
    GET_BOUND rax,0,0;get bound:compare-composite
    violation_check rax, T_CLOSURE
      REST_APPLIC
    jmp doneif43
    dif43:
    mov rax, [const_tbl + 16]
    doneif43:
    jmp doneif42
    dif42:
    mov rax, [const_tbl + 16]
    doneif42:
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    jne doneor4
		push SOB_NIL_ADDRESS
			mov rax, PVAR(0);get param:x
    push rax
    push qword 1
    GET_BOUND rax,1,12;get bound:vector?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif44
    push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:y
    push rax
    push qword 1
    GET_BOUND rax,1,12;get bound:vector?
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    je dif45
    push SOB_NIL_ADDRESS
			GET_BOUND rax,1,6;get bound:vector-length
    push rax
			GET_BOUND rax,1,5;get bound:vector-ref
    push rax
			mov rax, PVAR(1);get param:y
    push rax
			mov rax, PVAR(0);get param:x
    push rax
    push qword 4
    GET_BOUND rax,0,0;get bound:compare-composite
    violation_check rax, T_CLOSURE
      REST_APPLIC
    jmp doneif45
    dif45:
    mov rax, [const_tbl + 16]
    doneif45:
    jmp doneif44
    dif44:
    mov rax, [const_tbl + 16]
    doneif44:
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    jne doneor4
		push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:y
    push rax
			mov rax, PVAR(0);get param:x
    push rax
    
    GET_BOUND rax,1,13;get bound:eq?
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    jne doneor4
		mov rax, [const_tbl + 16]
    mov rbx, SOB_FALSE_ADDRESS
    cmp rbx, rax
    jne doneor4    
    doneor4:
    leave
    ret
		doneclousure72:
    leave
    ret
		doneclousure71:
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 1
    REST_APPLICTP
    leave
    ret
		doneclousure70:
    violation_check rax, T_CLOSURE
      REST_APPLIC
    mov qword [fvar_tbl + WORD_SIZE*37], rax;setting fvar:equal?
    mov rax, SOB_VOID_ADDRESS
		call write_sob_if_not_void

		push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 56]
    push rax
			mov rax, [const_tbl + 56]
    push rax
			mov rax, [const_tbl + 56]
    push rax
			mov rax, [const_tbl + 56]
    push rax
    push qword 4
    push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 56]
    push rax
			mov rax, [const_tbl + 56]
    push rax
    push qword 2
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody76)
		jmp doneclousure76
		lambody76:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 2
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody77)
		jmp doneclousure77
		lambody77:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 4
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(3);get param:l
    push rax
			mov rax, PVAR(2);get param:w
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*31
       mov rax, qword [rbx];get fvar:+
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:y
    push rax
			mov rax, PVAR(0);get param:x
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*31
       mov rax, qword [rbx];get fvar:+
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    
    mov rbx, fvar_tbl + WORD_SIZE*31
       mov rax, qword [rbx];get fvar:+
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    leave
    ret
		doneclousure77:
    leave
    ret
		doneclousure76:
    violation_check rax, T_CLOSURE
      REST_APPLIC
    violation_check rax, T_CLOSURE
      REST_APPLIC
		call write_sob_if_not_void

		push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 56]
    push rax
			mov rax, [const_tbl + 56]
    push rax
			mov rax, [const_tbl + 56]
    push rax
			mov rax, [const_tbl + 56]
    push rax
    push qword 4
    push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 56]
    push rax
			mov rax, [const_tbl + 56]
    push rax
    push qword 2
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody78)
		jmp doneclousure78
		lambody78:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 2
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody79)
		jmp doneclousure79
		lambody79:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 4
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			GET_BOUND rax,0,1;get bound:v
    push rax
			GET_BOUND rax,0,0;get bound:z
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*31
       mov rax, qword [rbx];get fvar:+
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			mov rax, PVAR(3);get param:l
    push rax
			mov rax, PVAR(2);get param:w
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*31
       mov rax, qword [rbx];get fvar:+
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			mov rax, PVAR(1);get param:y
    push rax
			mov rax, PVAR(0);get param:x
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*31
       mov rax, qword [rbx];get fvar:+
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*31
       mov rax, qword [rbx];get fvar:+
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    
    mov rbx, fvar_tbl + WORD_SIZE*31
       mov rax, qword [rbx];get fvar:+
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    leave
    ret
		doneclousure79:
    leave
    ret
		doneclousure78:
    violation_check rax, T_CLOSURE
      REST_APPLIC
    violation_check rax, T_CLOSURE
      REST_APPLIC
		call write_sob_if_not_void

		push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 56]
    push rax
    push qword 1
    push SOB_NIL_ADDRESS
			mov rax, [const_tbl + 56]
    push rax
			mov rax, [const_tbl + 56]
    push rax
			mov rax, [const_tbl + 56]
    push rax
    push qword 3
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody80)
		jmp doneclousure80
		lambody80:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 3
    EXTEND_VEC rax, rbx
    push rax
    COPY_PARAM rbx
		mov rbx, rax
		MAKE_CLOSURE(rax, rbx, lambody81)
		jmp doneclousure81
		lambody81:
		push rbp
    mov rbp, rsp
    LAMBDA_ARG_CHECK 1
    push SOB_NIL_ADDRESS
			push SOB_NIL_ADDRESS
			GET_BOUND rax,0,2;get bound:t
    push rax
			GET_BOUND rax,0,0;get bound:z
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*31
       mov rax, qword [rbx];get fvar:+
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
			push SOB_NIL_ADDRESS
			GET_BOUND rax,0,1;get bound:v
    push rax
			mov rax, PVAR(0);get param:x
    push rax
    push qword 2
    mov rbx, fvar_tbl + WORD_SIZE*31
       mov rax, qword [rbx];get fvar:+
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
      REST_APPLIC
    push rax
    
    mov rbx, fvar_tbl + WORD_SIZE*31
       mov rax, qword [rbx];get fvar:+
		IS_UNDEF rbx
    violation_check rax, T_CLOSURE
    TP_SHIFT_FRAME 2
    REST_APPLICTP
    leave
    ret
		doneclousure81:
    leave
    ret
		doneclousure80:
    violation_check rax, T_CLOSURE
      REST_APPLIC
    violation_check rax, T_CLOSURE
      REST_APPLIC
		call write_sob_if_not_void
	ret